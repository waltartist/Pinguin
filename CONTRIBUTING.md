# Contributing

Pinguin is in beta. Small, focused pull requests are preferred.

## Setup

```bash
npm install
npm run dev:all
```

## Before Opening a Pull Request

```bash
npm run check
```

For UI changes, follow [`docs/ember-style-guide.md`](docs/ember-style-guide.md).
Never commit credentials, Pi session data, generated logs, or local assistant
configuration.
